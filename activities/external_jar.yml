version: 1
job:
  type: "external"
config:
  endpoint: "databricks_1"
  properties:
    run_name: "name"
    timeout_seconds: 143
    task: "jar"
    task_properties:
      main_class: "mainClass.scala"
      parameters:
      - "abcd"
      - "pqrs"
    libraries:
    - type: "maven"
      coordinates: "maven_cor"
      repo: "maven_repo"
      exclusions:
      - "maven_excl"
folder: "/ / /"
